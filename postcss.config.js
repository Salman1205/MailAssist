// Clean Tailwind v4 PostCSS config. (Restored: origin/main had obfuscated
// malware appended here that executed on every build — see security note.)
module.exports = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
